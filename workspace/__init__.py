"""Shared workspace base: on-disk contract, env loading, and PDF bootstrap.

Everything here is depended on by the server, the light-weight inspection
tools, and every heavy tool engine. It belongs to no single tool.
"""
