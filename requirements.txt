# agent_backend runtime dependencies.
#
# The conversational agent (deepagents) requires the langchain / langgraph 1.x
# generation, so the whole project is unified on 1.x. The vendored inner
# pipeline only touches `from langgraph.graph import END, START, StateGraph`,
# which is stable on 1.x.

# --- conversational agent layer ---
deepagents==0.5.4
langchain>=1.3,<2.0
langchain-core>=1.4,<2.0
langchain-openai>=1.3,<2.0
langgraph>=1.2,<2.0

# --- persistence (Postgres) ---
# App metadata/session/turn tables + LangGraph checkpoints. Only needed when
# PPT_DATABASE_URL is set; the app runs in-memory / off-disk without them.
psycopg[binary]>=3.2,<4.0
psycopg-pool>=3.2,<4.0
langgraph-checkpoint-postgres>=2.0,<4.0

# --- FastAPI server ---
fastapi>=0.115,<1.0
uvicorn>=0.30,<1.0

# --- inner pipeline (PDF -> pages -> render) ---
playwright==1.59.0
PyMuPDF>=1.27
httpx>=0.27,<1.0
Pillow>=10.0,<12.0
lxml>=5.2
